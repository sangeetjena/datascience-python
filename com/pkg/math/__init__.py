from typing import List, Any


class factorial_cal:
    fact: List[Any] =[]
    def factorial(self,num):
        for i in range(1..num):
            res=num%i
            if res==0:
                factorial_cal.fact.append(res)
            else:
                i+=1
        return factorial_cal.fact